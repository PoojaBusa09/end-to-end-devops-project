apiVersion: v1
kind: Service
metadata:
  name: guestbook-ui
  namespace: end-to-end-devops-project
spec:
  type: NodePort
  selector:
    app: java-app
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
